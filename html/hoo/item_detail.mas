<%args>
	$itemH
</%args>
<%init>
	if ( $itemH->{ item_type_id } == Vencore::ITEM_TYPE_PAGE ) {
		$m->comp( '/site/item_detail_page.mas', %ARGS );
		}
	elsif ( $itemH->{ item_type_id } == Vencore::ITEM_TYPE_PRESS_RELEASE ) {
		$m->comp( '/site/item_detail_press_release.mas', %ARGS );
		}
</%init>